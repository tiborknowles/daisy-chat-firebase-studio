{
  "name": "daisy-chat-backend",
  "version": "1.0.0",
  "description": "Backend for DaisyAI Chat with SSE streaming",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "deploy": "gcloud run deploy daisy-chat-backend --source . --platform managed --region us-central1 --allow-unauthenticated"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "@google-cloud/aiplatform": "^3.24.0",
    "uuid": "^9.0.1",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}